# Анализ результата выполнения скрипта и дальнейшие действия

## ✅ Что успешно сделано

1. **Схема питания переключена на "Высокая производительность"** - это критически важно для максимальной производительности Intel Arc iGPU
2. **Настройки памяти уже были оптимальными** - LargePageMinimum, DisablePagingExecutive и SecondLevelDataCache установлены правильно
3. **Параметры Intel GMM настроены** (DedicatedSegmentSize=4096, DynamicMemoryManagement=0, PreallocatedVram=4096)

## ⚠️ Ключевое ограничение

**Обнаружено всего ~2 ГБ VRAM** вместо ожидаемых 4 ГБ. Это подтверждает, что **BIOS вашего ноутбука ограничивает выделение VRAM** - это типично для ноутбуков, так как производители часто блокируют настройку DVMT Pre-Allocated в BIOS.

## 📌 Что делать дальше (пошагово)

### 1. НЕ активируйте base окружение с Python 3.13!
Это **главная ошибка**, которую делают многие. Base окружение с Python 3.13 **не подходит** для работы с Intel Arc iGPU.

### 2. Создайте специальное окружение Conda для LLM с Python 3.11

Закройте текущий PowerShell (администратор) и откройте **обычный PowerShell (без прав администратора)**, затем выполните:

```powershell
# Создаем новое окружение с Python 3.11
conda create -n llm-arc python=3.11 libuv -y

# Активируем его
conda activate llm-arc

# Устанавливаем необходимые пакеты
pip install --pre --upgrade ipex-llm[xpu] --extra-index-url https://pytorch-extension.intel.com/release-whl/stable/xpu/us/
pip install transformers==4.42.0 accelerate==0.29.3 bitsandbytes==0.43.0 sentence-transformers==2.2.2 chromadb==0.3.23
```

### 3. Создайте стартовый скрипт для запуска

В папке вашего проекта создайте файл `start_optimized.ps1` со следующим содержимым:

```powershell
# Установка переменных окружения для Intel XPU
$env:SYCL_CACHE_PERSISTENT = "1"
$env:ENABLE_LP64 = "1"
$env:SYCL_DEVICE_FILTER = "level_zero:gpu"
$env:ONEAPI_DEVICE_SELECTOR = "level_zero:*"
$env:OMP_NUM_THREADS = "8"
$env:KMP_BLOCKTIME = "0"

# Создание кэша SYCL
$cacheDir = "$pwd\.sycl_cache"
if (-not (Test-Path $cacheDir)) {
    New-Item -Path $cacheDir -ItemType Directory -Force | Out-Null
}
$env:SYCL_CACHE_DIR = $cacheDir

# Запуск Python с высоким приоритетом
Start-Process -FilePath "python" -ArgumentList $args -NoNewWindow -Wait -PriorityClass High
```

### 4. Пример кода для работы с 2 ГБ VRAM

Создайте файл `run_llm.py`:

```python
import torch
from transformers import AutoModelForCausalLM, AutoTokenizer, BitsAndBytesConfig
import intel_extension_for_pytorch as ipex

# Оптимизация аллокатора памяти
torch.xpu.set_allocator(torch.xpu.XPUCachingAllocator)
torch.xpu.empty_cache()

# 4-битная квантование с оптимизациями для 2 ГБ VRAM
bnb_config = BitsAndBytesConfig(
    load_in_4bit=True,
    bnb_4bit_quant_type="nf4",
    bnb_4bit_compute_dtype=torch.bfloat16,
    bnb_4bit_use_double_quant=True,
    cpu_embedding=True  # КРИТИЧНО для экономии VRAM
)

# Загрузка ЛЕГКОЙ модели (обязательно до 3B параметров)
model_id = "microsoft/phi-2"  # Или "Qwen/Qwen2-1.5B-Instruct"
tokenizer = AutoTokenizer.from_pretrained(model_id)

model = AutoModelForCausalLM.from_pretrained(
    model_id,
    quantization_config=bnb_config,
    max_memory={"xpu": "1.8GB", "cpu": "8GB"},  # Оставляем запас для системы
    torch_dtype=torch.bfloat16,
    trust_remote_code=False,
    low_cpu_mem_usage=True
)

# Применение IPEX оптимизаций
model = ipex.llm.optimize(
    model,
    dtype=torch.bfloat16,
    inplace=True,
    auto_cast="matmul",  # Активация XMX инструкций
    graph_mode=True,     # Оптимизация графа вычислений
    weights_prepack=True # Уменьшение задержек доступа
)

# Пример генерации
prompt = "Как работает квантование 4-битных моделей?"
inputs = tokenizer(prompt, return_tensors="pt").to("xpu")
outputs = model.generate(**inputs, max_new_tokens=100)
print(tokenizer.decode(outputs[0], skip_special_tokens=True))
```

### 5. Запуск вашей модели

```powershell
# Перейдите в папку проекта
cd C:\путь\к\вашему_проекту


# 3. Устанавливаем зависимости
pip install --pre torch==2.7.1+xpu torchvision==0.22.1+xpu torchaudio==2.7.1+xpu \
  --index-url https://download.pytorch.org/whl/nightly/xpu
pip install ipex-llm[xpu] --extra-index-url https://pytorch-extension.intel.com/release-whl/stable/xpu/us/

)

# Активируйте окружение
conda activate lfp_bot_py311 через запуск скрипта D:\DEV\LFP-TGbot-LLM-RAG-Cursor\setup_env.ps1,
 или запуск скрипта D:\DEV\LFP-TGbot-LLM-RAG\start_bot.ps1

# Запустите через оптимизированный скрипт start_optimized.ps1(
# Установка переменных окружения для Intel XPU
$env:SYCL_CACHE_PERSISTENT = "1"
$env:ENABLE_LP64 = "1"
$env:SYCL_DEVICE_FILTER = "level_zero:gpu"
$env:ONEAPI_DEVICE_SELECTOR = "level_zero:*"
$env:OMP_NUM_THREADS = "8"
$env:KMP_BLOCKTIME = "0"

# Создание кэша SYCL
$cacheDir = "$pwd\.sycl_cache"
if (-not (Test-Path $cacheDir)) {
    New-Item -Path $cacheDir -ItemType Directory -Force | Out-Null
}
$env:SYCL_CACHE_DIR = $cacheDir

# Запуск Python с высоким приоритетом
Start-Process -FilePath "python" -ArgumentList $args -NoNewWindow -Wait -PriorityClass High

)
.\start_optimized.ps1 run_llm.py
```
()
## 💡 Важные замечания

1. **Не используйте Python 3.13 для LLM на Intel Arc** - поддержка torch-xpu для Python 3.12 и выше пока не полная
2. **Всегда запускайте через `start_optimized.ps1`** перед каждым запуском вашей модели
3. **Используйте только модели до 3B параметров** (Phi-2, Qwen2-1.5B) - с 2 ГБ VRAM модели больше 3B параметров не запустятся
4. **Обязательно используйте `cpu_embedding=True`** - это критично для экономии VRAM
5.**Используйте оптимизированный код для работы с 2 ГБ VRAM
(
import torch
from transformers import AutoModelForCausalLM, AutoTokenizer, BitsAndBytesConfig
import intel_extension_for_pytorch as ipex

# Оптимизация аллокатора памяти
torch.xpu.set_allocator(torch.xpu.XPUCachingAllocator)
torch.xpu.empty_cache()

# 4-битная квантование с оптимизациями для 2 ГБ VRAM
bnb_config = BitsAndBytesConfig(
    load_in_4bit=True,
    bnb_4bit_quant_type="nf4",
    bnb_4bit_compute_dtype=torch.bfloat16,
    bnb_4bit_use_double_quant=True,
    cpu_embedding=True  # КРИТИЧНО для экономии VRAM
)

# Загрузка ЛЕГКОЙ модели (до 3B параметров)
model_id = "microsoft/phi-2"  # Или "Qwen/Qwen2-1.5B-Instruct"
tokenizer = AutoTokenizer.from_pretrained(model_id)

model = AutoModelForCausalLM.from_pretrained(
    model_id,
    quantization_config=bnb_config,
    max_memory={"xpu": "1.8GB", "cpu": "8GB"},  # Оставляем запас для системы
    torch_dtype=torch.bfloat16,
    trust_remote_code=False,
    low_cpu_mem_usage=True
)

# Применение IPEX оптимизаций
model = ipex.llm.optimize(
    model,
    dtype=torch.bfloat16,
    inplace=True,
    auto_cast="matmul",  # Активация XMX инструкций
    graph_mode=True,     # Оптимизация графа вычислений
    weights_prepack=True # Уменьшение задержек доступа
)
)

## 📊 Ожидаемая производительность

С вашей конфигурацией (Intel Arc iGPU, 2 ГБ VRAM) вы должны получить:
- Скорость генерации: 8-12 токенов/сек для моделей до 2.7B параметров
- Потребление VRAM: ~1.8 ГБ (оставляя запас для системы)
- Поддержка моделей: до 3B параметров в 4-битном режиме

Это достаточная производительность для большинства практических задач с LLM, не требующих очень больших моделей.